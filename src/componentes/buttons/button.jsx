import styled from "styled-components"

export const Button = (props) => {

    return(
        <Buttonn {...props} />
    )
}

const Buttonn = styled.button`
    border: none;
    margin-right: 10px;
`